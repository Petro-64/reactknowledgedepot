import styled from 'styled-components'

const Styledtesting = styled.div`
    .answerBlock {
        background-color: #C2CEDF;
        padding: 15px;
        border: none;
        margin: 10px 0;
        cursor: pointer;
    }

    .answerBlock:hover {
        background-color: #a7b7cf;
        position: relative;
        top: -2px;
        left: -2px;
        -webkit-box-shadow: 4px 7px 7px -2px rgba(0,0,0,0.63); 
        box-shadow: 4px 7px 7px -2px rgba(0,0,0,0.63);
    }
`;

export default Styledtesting